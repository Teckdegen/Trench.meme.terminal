# Privy's @privy-io/react-auth has a transitive peer dep on permissionless
# which requires ox@^0.8.0, while modern viem ships ox@0.14+. Both versions
# coexist fine at runtime — this just tells npm to stop refusing the install.
# Same flag Vercel/Railway/Netlify all honor.
legacy-peer-deps=true
